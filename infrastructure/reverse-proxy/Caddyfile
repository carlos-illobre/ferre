# Caddy de DESARROLLO (perfil gateway): sirve la API en http://localhost. En la máquina de
# Oracle no se usa: ahí el gateway compartido es deployment/oracle-single/caddy-gateway.
{$SITIO} {
    encode gzip
    reverse_proxy gestion-del-local:8080
}
